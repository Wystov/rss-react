import { Component } from 'react';

class Search extends Component {
  render() {
    return (
      <section>
        <input type="text" />
        <button>Search</button>
      </section>
    );
  }
}

export default Search;
